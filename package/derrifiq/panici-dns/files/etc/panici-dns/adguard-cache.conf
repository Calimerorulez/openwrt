# Panici DNS -> AdGuard Home cache invalidation
#
# Credentials worden NIET in dit bestand opgeslagen.
# Gebruik /etc/panici-dns/adguard.netrc (0600).

ENABLED=1
URL="http://127.0.0.1:3000/control/cache_clear"
NETRC="/etc/panici-dns/adguard.netrc"
